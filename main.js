import _ from "lodash";
import myFunc from './file1'

const arr = [1,2,3]

console.log(myFunc())

console.log("harrison")

const lastItem = _.last(arr)
console.log(lastItem)

// npx

// runs executable file
// shorthand
// we don't to do the entire filepath to the executable